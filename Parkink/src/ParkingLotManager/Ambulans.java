package ParkingLotManager;
import ParkingLotManager.Entities.Car;
import ParkingLotManager.Entities.Interface2.Nothing;
public class Ambulans extends Car implements Nothing {

    public Ambulans(String plate) {
        super(plate);
    }
}
